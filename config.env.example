# Rundeck MCP Server Configuration
# Copy this file to .env and fill in your Rundeck details

# === Single Server Configuration (Backward Compatible) ===
# Rundeck server URL (without trailing slash)
RUNDECK_URL=https://your-rundeck-server.com

# Rundeck API token (generate from User Profile > User API Tokens)
RUNDECK_API_TOKEN=your-api-token-here

# Rundeck API version (optional, defaults to 47)
RUNDECK_API_VERSION=47

# === Multiple Server Configuration ===
# You can configure up to 9 additional Rundeck servers using numbered variables
# Each server needs at minimum a URL and API token

# Server 1 (Production)
#RUNDECK_URL_1=https://rundeck-prod.company.com
#RUNDECK_API_TOKEN_1=prod-api-token-here
#RUNDECK_API_VERSION_1=47
#RUNDECK_NAME_1=production

# Server 2 (Staging)
#RUNDECK_URL_2=https://rundeck-staging.company.com
#RUNDECK_API_TOKEN_2=staging-api-token-here
#RUNDECK_API_VERSION_2=47
#RUNDECK_NAME_2=staging

# Server 3 (Development)
#RUNDECK_URL_3=https://rundeck-dev.company.com
#RUNDECK_API_TOKEN_3=dev-api-token-here
#RUNDECK_API_VERSION_3=47
#RUNDECK_NAME_3=development

# Note: If you configure multiple servers, you can specify which server to use
# in each tool call with the "server" parameter. If not specified, the default
# server (single config) or first available server will be used.